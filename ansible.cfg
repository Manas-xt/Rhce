[defaults]
inventory = /home/admin/ansible/inventory
remote_user = admin
#ask_vault_pass = true
#ask_pass = true

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false 
